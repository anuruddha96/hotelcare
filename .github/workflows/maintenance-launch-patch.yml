name: Maintenance Launch Patch

on:
  push:
    branches:
      - fix/maintenance-supervisor-forwarded-i18n

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo maintenance-patch-runner-ok
