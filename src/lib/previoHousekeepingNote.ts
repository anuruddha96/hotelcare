// Shared with the SLNT server-side PMS sync so all properties parse Previo
// housekeeping department notes with identical safety rules.
export * from "../../supabase/functions/_shared/previoHousekeepingNote";
