# Optional next improvements

After real-device validation of this first pass, the next safe additions are per-room-type collapse/expand and a compact Market Intelligence drawer. Those are intentionally not mixed into the gesture fix so regressions are easier to isolate.
