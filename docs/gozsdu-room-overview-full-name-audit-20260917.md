# Gozsdu room overview: outstanding 17 September 2026

The 07:35 screenshot displays abbreviated room numbers (`13`, `16`, `17`, `21`, `25`, `33`, etc.) in the **Hotel Room Overview** while the Map and tooltip use canonical `gozsdu_housekeeping_room_registry.pms_room_name` labels. The previous PR #211 fixed Room settings, operational floor map, Auto Assign preview, excluded-room selector and print, but **did not modify `GozsduCourtRoomOverview.tsx`**. This is the concrete root cause of the user's remaining report, not a presumed old browser cache. The current component explicitly renders `room.room_number` in `renderChip` and groups its List view by `room.floor_number` as `F0`/`F1`, even though Gozsdu's manager mapping is by building/apartment.

Follow-up scope: display canonical names in all four overview buckets, title, drag contextual labels and toast without modifying room IDs or persisted numeric identifiers; group List view by actual manager-assigned housekeeping section (never infer physical building from `1B`/`2B` prefix); leave existing Map assignments, PMS snapshot reconciler, operating/unavailable counts and other hotels untouched. Verify source and build via GitHub. Production deploy and live iPhone UX require separate confirmation.

Previous-request audit: PR #209 implemented Gozsdu date-matched PMS roster, building-compatible routing, room size/bed/time-aware fairness and tomorrow snapshot guards. PR #211 implemented the other named display surfaces and Laundryner Done in-place update. Neither PR guarantees actual frontend deployment or live correctness. Screenshot's 25 checkout / 24 service and 07:30 PMS verification are consistent with the new dated roster, but don't independently prove the 82-room roster or all future syncs are correct. Do not update production room rows, assignments or unrelated hotels as part of this display patch.
